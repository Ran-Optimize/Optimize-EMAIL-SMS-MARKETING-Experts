# Gallery (email portfolio) served under /gallery on the main domain.
# Splat rule first so /gallery/ is proxied; the bare /gallery then gets a trailing slash
# (needed so the gallery's relative asset paths resolve under /gallery/).
/gallery/*          https://optimize-agency-email-portfolio.netlify.app/:splat        200
/gallery            /gallery/                                                         301
